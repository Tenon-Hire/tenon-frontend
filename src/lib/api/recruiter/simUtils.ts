import {
  getId,
  getNumber,
  getString,
  isRecord,
  recruiterBffClient,
} from './base';

export { getId, getNumber, getString, isRecord, recruiterBffClient };
export { toStringOrNull, toNumberOrNull, safeId } from './base';
